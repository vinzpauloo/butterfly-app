import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const RecommendedVideos = (props: Props) => {
  return (
    <View>
      <Text>RecommendedVideos</Text>
    </View>
  );
};

export default RecommendedVideos;

const styles = StyleSheet.create({});
