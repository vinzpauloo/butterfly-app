import { StyleSheet } from 'react-native'
import React from 'react'
import Container from 'components/Container'

type Props = {}

const OnAppExitScreen = (props: Props) => {
	return (
		<Container>
		</Container>
	)
}

export default OnAppExitScreen

const styles = StyleSheet.create({})